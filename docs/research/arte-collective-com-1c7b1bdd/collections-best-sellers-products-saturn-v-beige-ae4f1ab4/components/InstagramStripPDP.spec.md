# InstagramStrip (page-specific)

Same visual structure as the homepage's `root-8a5edab2/InstagramStrip.tsx` (eyebrow "Instagram",
heading, horizontal scroll strip of square photos with "@artecollective_" tag overlay), but with
the PDP's actual live copy: "Share your poster with @artecollective_" (with "artecollective_" in
italic orange accent font) instead of the homepage's "Share your poster: @artecollective_".
Reuses the same theme photo assets (`root-8a5edab2/images/theme/Story_*.png`) — no new asset
download needed.
